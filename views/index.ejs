<html>
    <title><%= title  %></title>
    <body>
        <% messages.forEach(function(message){ %>
            <h3><%= message.user %></h3>
            <p><%= message.text %></p>
            <p><%= message.added %></p>
        <% }); %>
    </body>
</html>